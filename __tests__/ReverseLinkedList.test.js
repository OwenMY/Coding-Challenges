const { linkedList1, reverseLinkedList } = require('./../LeetCodeOtherSolutions/ReverseLinkedList.js');

describe('Reverse linked list', () => {
  test('Should be a linked list', () => {
    expect(linkedList1).toBe(linkedList1);
  });
});