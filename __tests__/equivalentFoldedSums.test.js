const equivalentFoldedSums = require('./../Challenges/EquivalentFoldedSums.js');

describe('Equivalent Folded Sums', () => {
  test('Should be something', () => {
    expect(equivalentFoldedSums(2)).toBe(undefined);
  });
});
